#ifndef WIFI_CREDENTIALS_H
#define WIFI_CREDENTIALS_H

/**
 * WiFi認証情報設定ファイル（サンプル）
 *
 * 使用方法:
 * 1. このファイルを "wifi_credentials.h" にコピー
 * 2. 実際のSSIDとパスワードを設定
 * 3. wifi_credentials.h は .gitignore により Git 管理外
 */

// プライマリWiFi設定
#define WIFI_SSID                "your-wifi-ssid"
#define WIFI_PASSWORD            "your-wifi-password"

// バックアップWiFi設定（オプション）
#define WIFI_SSID1               "your-backup-ssid-1"
#define WIFI_PASSWORD1           "your-backup-password-1"

#define WIFI_SSID2               "your-backup-ssid-2"
#define WIFI_PASSWORD2           "your-backup-password-2"

// WiFi接続設定
#define WIFI_MAXIMUM_RETRY       5
#define WIFI_CONNECT_TIMEOUT_SEC 30

#endif // WIFI_CREDENTIALS_H
